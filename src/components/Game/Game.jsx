import React from "react";

function Game() {
  // Check if user is in a game,
  // if so, Check if game is active
  // if game is active, show game
  // if game is not active show
  //if not
  return <div>Game</div>;
}

export default Game;
